{
  "watch": ["src", ".env"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "ts-node -r tsconfig-paths/register src/index.ts",
  "legacyWatch": true,
  "polling": true,
  "env": {
    "NODE_ENV": "development"
  }
}
